<!DOCTYPE html>
<html>
<head>
    <title>Lista de Productos/Recetas</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        table, th, td {
            border: 1px solid #ddd;
        }
        th, td {
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .btn {
            background-color: #333;
            color: white;
            padding: 6px 10px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            border-radius: 4px;
        }
        .btn:hover {
            background-color: #ddd;
            color: black;
        }
    </style>
</head>
<body>
    <h1>Lista de Productos/Recetas</h1>
    <table>
        <tr>
            <th>Titulo</th>
            <th>Creador</th>
            <th>Fecha de creación</th>
            <th>Acciones</th>
        </tr>
        <% data.forEach(function(item) { %>
            <tr>
                <td><%= item.titulo %></td>
                <td><%= item.creador %></td>
                <td><%= item.fechaCreacion %></td>
                <td>
                    <a class="btn" href="/panel/editarReceta?id=<%= item.idReceta %>">Editar</a>
                    <a class="btn" href="/admin/eliminarReceta/<%= item.idReceta %>">Eliminar</a>
                </td>
            </tr>
        <% }); %>
    </table>
    <a class="btn" href="/panel/agregar">Agregar Nuevo</a>
</body>
</html>